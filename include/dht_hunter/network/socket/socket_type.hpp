#pragma once

namespace dht_hunter::network {

/**
 * @enum SocketType
 * @brief Defines the socket type (TCP or UDP).
 */
enum class SocketType {
    TCP,
    UDP
};

} // namespace dht_hunter::network
