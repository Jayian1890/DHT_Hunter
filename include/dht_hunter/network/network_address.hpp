#pragma once

#include "dht_hunter/types/endpoint.hpp"

namespace dht_hunter::network {

// Use the types from the Types module
using NetworkAddress = dht_hunter::types::NetworkAddress;
using EndPoint = dht_hunter::types::EndPoint;

} // namespace dht_hunter::network
