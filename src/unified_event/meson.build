# Unified Event System library

# No source files found in this directory
# Creating an empty library for now
unified_event_sources = []

unified_event_deps = [
  dht_hunter_types_dep,
  thread_dep,
]

# Create an interface library since there are no source files
dht_hunter_unified_event_lib = declare_dependency(
  include_directories: inc_dirs,
  dependencies: unified_event_deps,
)

dht_hunter_unified_event_dep = dht_hunter_unified_event_lib
