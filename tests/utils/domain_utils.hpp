#pragma once

// Include the utility headers for testing
#include "dht_hunter/utility/utility.hpp"

// Use the utility namespaces for easier access in tests
namespace dht_hunter::utils::tests {

// Import utility namespaces for easier access in tests
namespace network = dht_hunter::utility::network;
namespace node = dht_hunter::utility::node;
namespace metadata = dht_hunter::utility::metadata;

} // namespace dht_hunter::utils::tests
