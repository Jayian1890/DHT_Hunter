# Types module tests

# NodeID tests
test_node_id = executable('test_node_id',
  'test_node_id.cpp',
  include_directories: inc_dirs,
  dependencies: [
    dht_hunter_types_dep,
  ],
)

test('NodeID Test', test_node_id)

# InfoHash tests
test_info_hash = executable('test_info_hash',
  'test_info_hash.cpp',
  include_directories: inc_dirs,
  dependencies: [
    dht_hunter_types_dep,
  ],
)

test('InfoHash Test', test_info_hash)

# Endpoint tests
test_endpoint = executable('test_endpoint',
  'test_endpoint.cpp',
  include_directories: inc_dirs,
  dependencies: [
    dht_hunter_types_dep,
  ],
)

test('Endpoint Test', test_endpoint)
